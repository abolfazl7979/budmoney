// added a custom history to routing so i can use history to navigate the user after logging in, in the index.js file.
import { createBrowserHistory } from 'history';
export default createBrowserHistory();